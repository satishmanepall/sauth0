# sauth0-api
The SAUTH0 API is a dedicated service for authentication and authorization.JWT-based  access token management.Refresh token mechanisms to maintain active sessions. Role-based access control for different user types,OTP 
