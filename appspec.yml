version: 0.0
os: linux
files:
  - source: /
    destination: /opt/codedeploy-agent/deployment-root/${SAUT0-API-DEPLOYMENT-ID}/deployment-archive
hooks:
  AfterInstall:
    - location: scripts/after_install.sh
      timeout: 300
      runas: proddeployuser
